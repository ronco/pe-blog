<h1>All Posts</h1>
<ul>
  {{#each model as |post|}}
    {{#link-to 'post' post}}
      <li>
        <h3>{{post.title}}</h3>
        <p>Posted at {{post.date}} by {{post.author.name}}</p>
      </li>
    {{/link-to}}
  {{/each}}
</ul>
